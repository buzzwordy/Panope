apiVersion: v1
kind: Service
metadata:
  name: {{ include "panope.fullname" . }}
  labels: {{- include "panope.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "panope.selectorLabels" . | nindent 4 }}
